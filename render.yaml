services:
  - type: web
    name: quizworld-realtime
    runtime: elixir
    rootDir: services/quizworld_realtime
    buildCommand: mix deps.get && mix compile
    startCommand: mix phx.server
    envVars:
      - key: MIX_ENV
        value: prod
      - key: PORT
        value: 4000
